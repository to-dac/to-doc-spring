# MySQL 연결 설정
DB_URL=jdbc:mysql://localhost:3306/todoc?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
DB_USERNAME=root
DB_PASSWORD=your_password

# JPA DDL 전략 (validate | update | create | create-drop)
JPA_DDL_AUTO=validate

# 서버 포트
SERVER_PORT=8080
